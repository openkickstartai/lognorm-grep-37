# lognorm-grep-37
每个上过规模的代码库都有同一个隐疾：日志写得多，但可观测性反而更差。logger.info('user logged in')、logger.info('User login successful')、logger.debug('Login completed for user') 散落在三个模块里，生产出事时你 grep 关键词命中一屏幕结果，根本不知道哪条才是你要找的那行。�...
